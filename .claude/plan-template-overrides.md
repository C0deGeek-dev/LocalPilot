# Plan-Template Overrides — LocalPilot (pointer)

LocalPilot's real plan-template overrides live at
[`.agents/skills/plan-large-task/plan-template-overrides.md`](../.agents/skills/plan-large-task/plan-template-overrides.md),
named by this repo's `plan-large-task` skill.

This stub exists so a cross-project plan (scaffolded from another repo's cwd)
that splices `.claude/plan-template-overrides.md` by convention still discovers
LocalPilot's overrides — read the `.agents/…` file above, do not duplicate its
content here.
