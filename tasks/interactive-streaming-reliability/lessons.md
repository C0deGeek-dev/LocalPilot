# Lessons

- High-rate model events must not determine terminal redraw or keyboard polling frequency.
- A compatible stop reason is insufficient when a started content block remains open.
- Retrying a degenerate local-model response without tool schemas is a conservative compatibility fallback.
