{
  "id": "classify-token",
  "entry": "solution.rs",
  "problem": "classify(token) sorts a token into Empty, Number, or Word, but it checks for a numeric token before the empty case, so an empty string is reported as a Word and the test empty_is_empty fails. Order the checks so an empty token is classified Empty first."
}
