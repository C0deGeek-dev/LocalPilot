[package]
name = "localpilot-llm-anthropic"
version = "3.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[lints]
workspace = true

[features]
# Exposes doc-hidden entry points that drive the SSE decoder over arbitrary
# bytes. Only the fuzz harness enables this; it adds no dependencies.
fuzzing = []

[dependencies]
async-trait = { workspace = true }
futures = { workspace = true }
indexmap = { workspace = true }
reqwest = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }
localpilot-core = { path = "../localpilot-core" }
localpilot-llm-core = { path = "../localpilot-llm-core" }
